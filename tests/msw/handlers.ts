import { http, HttpResponse } from 'msw';

// Add/expand as client utilities need network mocks
export const handlers = [
  // Example:
  // http.get('/api/breed', () => HttpResponse.json({ name: 'Labrador' })),
];




